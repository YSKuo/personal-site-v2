export const AboutHero = ({ 
  textAboveHeading = 'Hello!',
  heading = 'Heading', 
  desc = 'Desc', 
  img, href, linkText = 'link text', children }) => (
  <Section py="8|12|16|24">
    <Container>
      <Grid col={img && "1|2"} gap="8|12|16" alignItems="center">
        {img && (
          <Image
            src={img}
            colStart="null|2"
            w="full"
            rounded="lg"
            overflow="hidden"
          />
        )}
        <Flexbox
          flexDirection="column"
          alignItems="center|flex-start"
          textAlign="center|left"
        >
          <P>{textAboveHeading}</P>
          <H1 m="0" fontWeight="extrabold" lineHeight="tight">
            {heading}
          </H1>
          {children}
          {href && (
            <ButtonLink href={href} variant="primary lg">
              {linkText}
              <Icon name="arrow-right" ml="4" />
            </ButtonLink>
          )}
        </Flexbox>
      </Grid>
    </Container>
  </Section>
)