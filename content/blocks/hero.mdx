export const Hero = ({ 
  textAboveHeading = 'Hello!',
  heading = 'Heading', 
  desc = 'Desc', 
  img, href, linkText = 'link text' }) => (
  <Section py="8|12|16|24">
    <Container>
      <Grid col={img && "1|2"} gap="8|12|16" alignItems="center">
        {img && (
          <Image
            src={img}
            colStart="null|2"
            w="full"
            rounded="lg"
            overflow="hidden"
          />
        )}
        <Flexbox
          flexDirection="column"
          alignItems="center|flex-start"
          textAlign="center|left"
        >
          <p>{textAboveHeading}</p>
          <H1 m="0" fontWeight="extrabold" lineHeight="tight">
            {heading}
          </H1>
          <P fontSize="xl|2xl" mt="2">
            {desc}
          </P>
          {href && (
            <ButtonLink href={href} variant="primary">
              {linkText}
              <Icon name="arrow-right" ml="4" />
            </ButtonLink>
          )}
        </Flexbox>
      </Grid>
    </Container>
  </Section>
)